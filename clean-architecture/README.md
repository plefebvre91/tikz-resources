# Clean Architecture Poster

## Compilation

1. the following tools must be installed to compile the beamer: `inkscape`, [`plantuml`](https://plantuml.com/fr);
2. update the `plantuml` path in the makefile;
3. run `make`
